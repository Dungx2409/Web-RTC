# Server Environment Configuration
# Copy to .env and update values

# Server Port
PORT=3001

# Use HTTPS (set to true for production)
USE_HTTPS=false

# SSL Certificate paths (required when USE_HTTPS=true)
CERT_PATH=./cert.pem
KEY_PATH=./key.pem
